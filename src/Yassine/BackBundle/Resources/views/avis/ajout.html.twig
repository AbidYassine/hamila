{% extends 'base.html.twig' %}

{% block body %}
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.8.0/jquery.min.js"></script>
    {% include '@FOSComment/Thread/async.html.twig' with {'id':  app.user.id  } %}

    <br>
    idoffre:<br>
    <input type="text" id="idoff">
    <br>
<span onmouseover="starmark(this)" onclick="result()" id="1one"  style="font-size:40px;cursor:pointer;" class="fa fa-star checked"></span>
<span onmouseover="starmark(this)" onclick="result()" id="2one"  style="font-size:40px;cursor:pointer;" class="fa fa-star "></span>
<span onmouseover="starmark(this)" onclick="result()" id="3one"  style="font-size:40px;cursor:pointer;" class="fa fa-star "></span>
<span onmouseover="starmark(this)" onclick="result()" id="4one"  style="font-size:40px;cursor:pointer;" class="fa fa-star"></span>
<span onmouseover="starmark(this)" onclick="result()" id="5one"  style="font-size:40px;cursor:pointer;" class="fa fa-star"></span>
<br/>
<button  onclick="result()" type="button" style="margin-top:10px;margin-left:5px;" class="btn btn-lg btn-success">Submit</button>

</body>
<script>
    var idoff;
    var count;
    var client ;
    client = '{{ app.user.id }}';
    alert('Client = '+client);
    idoff  =document.getElementById('idOffre').value;
    function starmark(item)
    {
        count=item.id[0];
        sessionStorage.starRating = count;
        var subid= item.id.substring(1);
        for(var i=0;i<5;i++)
        {
            if(i<count)
            {
                document.getElementById((i+1)+subid).style.color="orange";
            }
            else
            {
                document.getElementById((i+1)+subid).style.color="black";
            }
        }
    }
    function result()
    {
//Rating : Count
//Review : Comment(id)
        //alert("Rating : "+count);
       // alert("Rating : "+client+idoff+commentaiiire);
        window.location.pathname="/hamila/web/app_dev.php/ajoutavis/"+count+"/"+client+"/"+idoff+"/"+" ";
    }
</script>
    <form class="mb-3" method="post" >
<input onclick="Routing.generate('yassine_back_avis_ajout2', { 'id': count })" type="submit" value="Create" />
    </form>

{% endblock %}